[Service]
# One-off repair batches own the crawler host while active. Skip the nightly
# tick instead of competing for DNS and browser resources.
ExecCondition=/bin/bash -c '! systemctl --user is-active --quiet kiko-price-backfill.service && ! systemctl --user is-active --quiet kiko-product-images.service'
