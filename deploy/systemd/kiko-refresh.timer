# 한 런이 끝난 뒤 15분 쉬고 다음 런을 시작한다. 동일 service는 중복 실행되지
# 않으므로 새벽에만 몰지 않고 낮에도 source queue를 계속 순환한다.
[Unit]
Description=Continuous kiko.ai listing refresh (stock/price)

[Timer]
OnBootSec=10min
OnUnitInactiveSec=15min
AccuracySec=1min
Unit=kiko-refresh.service

[Install]
WantedBy=timers.target
